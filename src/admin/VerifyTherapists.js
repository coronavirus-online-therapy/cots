import React from 'react';
import Grid from '@material-ui/core/Grid';

function VerifyTherapists() {
  // TODO: fetch all accesspoints that are unverified
  // TODO: display name, email, state, license #, button to view, button to confirm
  // TODO: on confirm, update verification status and requery
  return (
    <Grid container spacing={3} justify="center">
      <Grid item xs={12}>
        <h1>hello2</h1>
      </Grid>
    </Grid>
  );
}

export default VerifyTherapists;
